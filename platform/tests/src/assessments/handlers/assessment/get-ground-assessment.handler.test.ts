/**
 * GetGroundAssessment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: assessments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getGroundAssessment } from "@forgetcase/api-server/domains/assessments/handlers";
import type { AssessmentsDDDDependencies } from "@forgetcase/api-server/domains/assessments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@forgetcase/tests/shared";
import { ASSESSMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetGroundAssessment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AssessmentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AssessmentsDDDDependencies>(
      ASSESSMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetGroundAssessment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { caseId: entity.id };

    await getGroundAssessment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetGroundAssessment.execute).toHaveBeenCalledWith(expect.objectContaining({ caseId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { caseId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetGroundAssessment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getGroundAssessment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
