/**
 * GetErasureCase handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getErasureCase } from "@forgetcase/api-server/domains/cases/handlers";
import type { CasesDDDDependencies } from "@forgetcase/api-server/domains/cases";
import { createErasureCase as createErasureCaseEntity } from "@forgetcase/factories/cases";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@forgetcase/tests/shared";
import { CASES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetErasureCase", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CasesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CasesDDDDependencies>(
      CASES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createErasureCaseEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetErasureCase, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { caseId: entity.id };

    await getErasureCase(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetErasureCase.execute).toHaveBeenCalledWith(expect.objectContaining({ caseId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { caseId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetErasureCase, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getErasureCase(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
