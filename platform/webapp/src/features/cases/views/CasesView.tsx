/**
 * Cases feature view — product desk entry is routed via App pages.
 * Kept as a thin re-export for codegen feature scaffolding.
 */

export { CaseDeskPage as CasesView } from '@/pages/CaseDeskPage';
export type { } from './types';
